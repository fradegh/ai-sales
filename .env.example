# AI Sales Operator - Environment Configuration
# Copy this file to .env and fill in your values

# ============ REQUIRED ============

# OpenAI API Configuration
AI_INTEGRATIONS_OPENAI_API_KEY=your_openai_api_key_here
AI_INTEGRATIONS_OPENAI_BASE_URL=https://api.openai.com/v1

# Session Secret (generate a random string)
SESSION_SECRET=your_session_secret_here_min_32_chars

# Master key for encrypting integration secrets (API keys, tokens)
# Generate with: openssl rand -base64 32
INTEGRATION_SECRETS_MASTER_KEY=your_32_byte_base64_key_here

# ============ ENVIRONMENT ============

# Environment: development | staging | production
NODE_ENV=development

# Server port (default: 5000)
PORT=5000

# Trust proxy (for Cloudflare/Nginx reverse proxy)
# Required for secure cookies behind reverse proxy
TRUST_PROXY=true

# ============ DATABASE (for future PostgreSQL migration) ============

# PostgreSQL connection URL
# DATABASE_URL=postgresql://user:password@localhost:5432/ai_sales_operator

# ============ FEATURE FLAGS (can be overridden via API) ============

# Enable AI suggestions (default: true)
# FEATURE_AI_SUGGESTIONS_ENABLED=true

# Enable RAG context retrieval (default: true)
# FEATURE_RAG_ENABLED=true

# Enable auto-send without approval (default: false)
# FEATURE_AI_AUTOSEND_ENABLED=false

# Enable human-like delay (default: false)
# FEATURE_HUMAN_DELAY_ENABLED=false

# Enable decision engine (default: false)
# FEATURE_DECISION_ENGINE_ENABLED=false

# ============ RATE LIMITING ============

# Max API requests per minute (default: 100)
# RATE_LIMIT_MAX_REQUESTS=100

# Max AI generation requests per minute (default: 20)
# RATE_LIMIT_AI_MAX_REQUESTS=20

# ============ LOGGING ============

# Log level: debug | info | warn | error (default: info)
# LOG_LEVEL=info

# ============ TELEGRAM BOT (optional) ============

# Telegram Bot Token
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Telegram Webhook Secret
# TELEGRAM_WEBHOOK_SECRET=your_webhook_secret

# ============ TELEGRAM PERSONAL (optional) ============
# For connecting personal Telegram accounts via MTProto
# Get these from https://my.telegram.org → API development tools

# Telegram API ID (numeric)
# TELEGRAM_API_ID=12345678

# Telegram API Hash (32 character hex string)
# TELEGRAM_API_HASH=0123456789abcdef0123456789abcdef

# ============ WHATSAPP (optional) ============

# WhatsApp Business API Token
# WHATSAPP_API_TOKEN=your_whatsapp_token

# WhatsApp Phone Number ID
# WHATSAPP_PHONE_ID=your_phone_id

# ============ VEHICLE LOOKUP (Podzamenu) ============

# URL of the Podzamenu lookup service (FastAPI, default: http://localhost:8200)
# PODZAMENU_LOOKUP_SERVICE_URL=http://localhost:8200

# ============ PRICE ENGINE (external sources) ============

# Enable Avito as price source (default: false)
# AVITO_ENABLED=true

# Enable Drom as price source (default: false)
# DROM_ENABLED=true

# SerpAPI key for web search price source (leave empty to disable)
# SERP_API_KEY=

# OpenAI model used for web search price lookups (uses web_search_preview tool)
# Default: gpt-4o-search-preview
# OPENAI_WEB_SEARCH_MODEL=gpt-4o-search-preview

# ============ SENTRY (optional, for error tracking) ============

# Sentry DSN
# SENTRY_DSN=https://your_sentry_dsn
